#include <stdlib.h>
#include <stdio.h>

// A prime number is a natural number greater than 1 and is completely divisible only by 1 and itself. 
// In this article, we will learn how to check whether the given number is a prime number or not in C.

int main(int argc, char const *argv[])
{
    int number;
    printf("We will be checking if an integer is prime in this program.\nPlease enter an integer: ");
    scanf("%d", number);
    
    return 0;
}
